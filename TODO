TODO

- documentation

- each(v, k) vs each(k, v)

- npm publish (need license)


/**
 * SELECTORS
 * - :visible, etc
 *
 * STYLE
 * - .css() - diff with jQuery in computed width / height
 * - .computedCSS()
 * - .width()
 * - .height()
 *
 * TRIGGER
 * - custom events
 *
 */
